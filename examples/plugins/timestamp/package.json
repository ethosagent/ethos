{
  "name": "ethos-plugin-timestamp",
  "version": "1.0.0",
  "private": true,
  "description": "Example: tool-only plugin — returns current time in any timezone",
  "type": "module",
  "main": "./src/index.ts",
  "ethos": {
    "type": "plugin",
    "pluginApi": "1.0.0"
  },
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@ethosagent/plugin-sdk": "workspace:*",
    "@ethosagent/types": "workspace:*"
  },
  "devDependencies": {
    "@ethosagent/core": "workspace:*",
    "vitest": "^4.1.9"
  }
}
