{
  "name": "ethos-plugin-memory-logger",
  "version": "1.0.0",
  "private": true,
  "description": "Example: memory-style plugin — logs every agent_done event to a local file",
  "type": "module",
  "main": "./src/index.ts",
  "ethos": {
    "type": "plugin",
    "pluginContractMajor": 4,
    "pluginApi": "1.0.0"
  },
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@ethosagent/plugin-sdk": "workspace:*",
    "@ethosagent/types": "workspace:*"
  },
  "devDependencies": {
    "@ethosagent/core": "workspace:*",
    "vitest": "^4.1.10"
  }
}
