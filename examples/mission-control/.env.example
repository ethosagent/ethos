# Public — safe to expose in client bundle (just a URL, not a secret)
NEXT_PUBLIC_ETHOS_BASE_URL=http://localhost:3000

# Server-side only — NEVER use NEXT_PUBLIC_ prefix for secrets
ETHOS_API_KEY=sk-ethos-your-key-here
ETHOS_BASE_URL=http://localhost:3000
